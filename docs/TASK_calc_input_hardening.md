# ТЗ: правильность расчётов и валидация входов (P1)

**Источник:** `REPORT_iskra_audit_2026-09-17.md`, §2.1, §2.2, §2.3, §2.4, §2.10.
**Дата:** 2026-09-22.

Все числа ниже получены прогоном модулей в Node 22.09.2026, кроме §2.4, который воспроизвести не удалось (см. шаг 0). Где цифра расходится с отчётом Искры, верной считать эту.

Почему это важнее остальных открытых пунктов: дыру в подписке найдут единицы, а неправильную цену увидит каждый.

---

## Шаг 0. §2.4 воспроизведён, причина найдена

**Статус на 22.09.2026: дефект подтверждён прогоном, но причина в отчёте Искры названа неверно.** Этот раздел переписан по фактическим числам, полученным на реальных данных. Ничего перепроверять заново не нужно, можно сразу чинить.

### Что происходит

Референсная площадь секций `S1` в `src/data/office-vis-data.js` равна 19 225,85 м², то есть ровно той площади, на которой снималась смета Технопарка. При этой площади соотношение `ratio = area / refArea` равно единице, и `Math.ceil(qOrig × 1)` возвращает исходное целое количество. Введите 19 226, соотношение станет 1,0000078, и каждая дискретная позиция получит лишний целый экземпляр.

Числа по `S1`, тир Стандарт:

| Площадь, м² | ₽/м² |
|---|---|
| 19 225,85 | 32 200 |
| 19 226 | **39 995** |

Плюс 24,2% на разнице в 15 сантиметров.

**Где именно рождаются деньги.** Разложение скачка в 149,5 млн ₽ по типам позиций:

| Тип | Позиций | Вклад в скачок |
|---|---|---|
| Площадь | 262 | 412 188 ₽ (0,2%) |
| Шт | 796 | 149 467 373 ₽ (99,8%) |

По исходному количеству:

| qOrig | Позиций | Лишние деньги | Доля |
|---|---|---|---|
| 1 | 381 | 105 036 433 ₽ | 70,3% |
| 2–3 | 89 | 21 327 074 ₽ | 14,3% |
| 4–10 | 131 | 20 227 871 ₽ | 13,5% |
| 11–50 | 135 | 2 369 953 ₽ | 1,6% |
| 51+ | 60 | 502 976 ₽ | 0,3% |

Семьдесят процентов скачка дают позиции, существующие в единственном экземпляре. Пример: монтаж видеосервера, `qOrig = 1`, цена 10 469 619 ₽/шт. Соотношение 1,0000078 превращает один сервер в два. Ни одна щитовая и ни один ИТП не удваиваются от того, что офис на пятнадцать сантиметров просторнее.

**Почему рекомендация аудита не сработала бы.** Отчёт предлагал оставить `Math.ceil` только для штук и комплектов. Но проблемные позиции и есть штуки (769) и комплекты (27), все до одной дискретные. А у позиций типа «Площадь» единицы как раз непрерывные: 260 в погонных метрах, одна в метрах, одна в килограммах. Разделение по единицам измерения снимает 0,2% проблемы.

### Второе открытие: половина механизма уже написана

В `B2BOfficeDetailPage.jsx` (якорь `rt === 'Расценки'`) есть ровно тот способ, который нужен единичным позициям: количество фиксируется, а единичная расценка тянется по площади.

```js
} else if (rt === 'Расценки') {
  vol = qO;
  pw = Math.round(it.pw * ratio * 100) / 100;
  pm = Math.round(it.pm * ratio * 100) / 100;
}
```

Разметка типов по наборам данных:

| Набор | Позиций | Типы |
|---|---|---|
| ВИС Стандарт | 1636 | Площадь 487, Шт 1149 |
| ВИС Бизнес | 2075 | Минимум 342, Шт 571, Площадь 292, Фикс 10, **Расценки 350**, Количество 510 |
| Отделка Стандарт | 534 | тип не проставлен |
| Отделка Бизнес | 534 | тип не проставлен |

«Бизнес» размечен шестью типами, «Стандарт» двумя. Работа по классификации была сделана для одного набора и не перенесена на другой. При этом «Бизнес» тоже скачет на границе своего эталона 43 671,88 м²: 51 117 → 53 601 ₽/м², плюс 4,9%. Классификация уменьшила скачок впятеро, но не убрала, потому что `Math.ceil` остался в ветках «Шт» и «Минимум».

### Решения владельца от 22.09.2026

**1. Правило округления.** Для дискретных позиций (`Шт`, `Минимум`) считать `Math.round` вместо `Math.ceil`, с полом в одну единицу: если в эталонной смете позиция была хотя бы в одном экземпляре, меньше одного не даём. Для непрерывных (`Площадь` и всё, что в погонных метрах, метрах, квадратных метрах, килограммах) держать дробный объём, округляя до сотых.

**2. Нижняя граница применимости: 10 000 м².** Верхней границы нет, офисы бывают любого размера.

**3. Ниже границы считаем, но предупреждаем заметно.** Не серой сноской.

### Что сделать в коде

В `B2BOfficeDetailPage.jsx`, якорь `if (rt === 'Площадь')`, привести ветки к виду:

```js
if (rt === 'Площадь') {
  vol = Math.round(qO * ratio * 100) / 100;          // непрерывная величина
} else if (rt === 'Шт' || rt === 'Минимум') {
  vol = Math.max(mn || (qO >= 1 ? 1 : 0), Math.round(qO * ratio / kr) * kr);
} else if (rt === 'Фикс') {
  vol = qO;
} else if (rt === 'Расценки') {
  vol = qO;
  pw = Math.round(it.pw * ratio * 100) / 100;
  pm = Math.round(it.pm * ratio * 100) / 100;
} else {
  vol = Math.round(qO * ratio * 100) / 100;
}
```

Изменения против текущего кода: `Math.ceil` уходит из трёх веток, в дискретной появляется пол в одну единицу, в `Фикс` пропадает бессмысленный `Math.ceil` от целого.

Проверка после правки, кривая вокруг эталона Стандарта должна идти без ступеньки: 19 225 → 32 196, 19 225,85 → 32 195, 19 226 → 32 194, 19 227 → 32 193.

### Что сделать в интерфейсе

Над таблицей результата, когда `params.S1 > 0 && params.S1 < 10000`, показывать блок в рамке с заметным фоном (не `C.gray400` и не мелкий шрифт), текст по смыслу такой:

> Расчёт масштабируется от сметы объекта площадью 19 226 м². На вашей площади удельная стоимость завышена примерно на N%. Для ориентировочной оценки используйте предварительный расчёт.

Процент считать не на глаз, а по таблице ниже, либо интерполяцией. Фактические отклонения от эталонной удельной цены после правки:

| Площадь, м² | Стандарт | Бизнес |
|---|---|---|
| 1 000 | +414% | +269% |
| 2 000 | +185% | +126% |
| 3 000 | +110% | +80% |
| 5 000 | +54% | +43% |
| 7 500 | +29% | +25% |
| 10 000 | +16% | +16% |
| 12 500 | +10% | +11% |
| 15 000 | +5% | +8% |

Вторая ступень: при `params.S1 < 3000`, где отклонение переваливает за сотню процентов, текст плашки жёстче — цифра носит справочный характер, для объекта такого размера детальная смета неприменима.

Ссылку «предварительный расчёт» вести на `/b2b-office`.

### Что остаётся владельцу, не Claude Code

Классификация 1636 позиций набора «Стандарт» по шести типам, как размечен «Бизнес». Это сметная работа, а не программирование: решить по каждой позиции, масштабируется она количеством, расценкой, минимумом или вовсе фиксирована. После правки округления скачок исчезнет и без классификации, но единичное дорогое оборудование по-прежнему будет масштабироваться количеством там, где правильнее тянуть расценку. Выносится отдельной задачей.

---

## §2.1 `formatRub` падает на пустом значении

### Факты прогона

```
formatRub(undefined) → TypeError: Cannot read properties of undefined (reading 'toLocaleString')
formatRub(null)      → TypeError: Cannot read properties of null (reading 'toLocaleString')
formatRub(NaN)       → "не число ₽"
formatRub(Infinity)  → "Infinity млн ₽"
formatRub(1234567)   → "1,23 млн ₽"
```

Падение на `null` в отчёте Искры не упомянуто, а оно важнее: см. связку в §2.2.

Якорь: `export function formatRub(n)` в `src/lib/calculator.js`.

### Что сделать

В начало функции добавить:

```js
if (!Number.isFinite(n)) return '—';
```

Это закрывает `undefined`, `null`, `NaN` и обе бесконечности одной строкой. Строку `'не число ₽'` из текущей ветки убрать, она теперь недостижима.

Отдельно: найти по `grep -rn "rpkm-last-b2c\|sessionStorage.getItem" src/` все чтения результатов расчёта из `sessionStorage` и обернуть `JSON.parse` в `try/catch`. Испорченное или устаревшее содержимое хранилища не должно ронять страницу результата.

---

## §2.2 Мусор на входе даёт смету из ничего и отрицательные деньги

### Факты прогона

Детальная смета, `SpecCalc.compute`, при санузлах 1, окнах 4, комнатах 2, тир «капитальный»:

| Площадь на входе | Что взял калькулятор | Итог |
|---|---|---|
| `0` | 1 м² | 1 526 890 ₽ |
| `-50` | 1 м² | 1 526 890 ₽ |
| `NaN` | 1 м² | 1 526 890 ₽ |
| `'abc'` | 1 м² | 1 526 890 ₽ |
| `undefined` | 1 м² | 1 526 890 ₽ |
| `1e9` | 1 000 000 000 м² | 59 205 442 802 238 ₽ |

Полтора миллиона рублей на один квадратный метр получается потому, что часть позиций не зависит от площади и считается комплектом.

Квиз, `calculateB2C`, те же входные:

| Площадь на входе | `totalLow` |
|---|---|
| `0` | 2 271 180 ₽ (молча подставлено 60 м²) |
| `'abc'` | 2 271 180 ₽ |
| `undefined` | 2 271 180 ₽ |
| `-50` | **−2 163 000 ₽** |
| `1e9` | 36 050 000 000 000 ₽ |

Якоря: `Math.max(1, parseFloat(area) || 0)` в `src/lib/spec-calculator.js`, `parseFloat(answers.area) || 60` в `src/lib/calculator.js`.

**Связка с §2.1, которой нет в отчёте.** `calculateB2B` при `bathrooms: 'abc'` возвращает не NaN, как утверждал аудит, а `null` во всех денежных полях: `lowPerM2: null, highPerM2: null, totalLow: null, totalHigh: null`. Дальше `null` попадает в `formatRub`, та бросает `TypeError`, и страница результата умирает белым экраном. То есть §2.1 и §2.2 это не два независимых мелких дефекта, а одна цепочка от мусора на входе до упавшего интерфейса.

Якорь: `parseInt(answers.bathrooms || 1)` в `src/lib/calculator.js`.

### Что сделать

**Единый валидатор.** Завести в `src/lib/calculator.js` и экспортировать:

```js
export function validateNumber(v, { min, max, name }) {
  const n = typeof v === 'number' ? v : parseFloat(String(v ?? '').replace(',', '.'));
  if (!Number.isFinite(n)) return { ok: false, error: `${name}: введите число` };
  if (min != null && n < min) return { ok: false, error: `${name}: не меньше ${min}` };
  if (max != null && n > max) return { ok: false, error: `${name}: не больше ${max}` };
  return { ok: true, value: n };
}
```

Запятая как разделитель обрабатывается намеренно: русскоязычный пользователь напишет «62,5».

**Принцип, важнее конкретных границ.** На невалидном входе расчёт не запускается и в интерфейсе появляется сообщение. Молчаливая подстановка дефолта запрещена: пользователь не должен получить цифру, которую он не заказывал. Сейчас `|| 60` и `Math.max(1, ...)` делают ровно это.

**Границы. Решение владельца от 22.09.2026.** Верхних границ для офисных калькуляторов не ставим, офисы бывают любого размера. Правило: число должно быть конечным и строго больше нуля. Ноль, отрицательные значения и нечисловой мусор отвергаются с сообщением в интерфейсе. Верхняя граница остаётся только там, где уже есть, в квизе B2C (`min:20, max:250`, якорь `type:'area'` в `src/pages/B2CQuizPage.jsx`). Для счётных полей (санузлы, окна, комнаты, рабочие места, переговорные) правило то же: целое, конечное, не меньше нуля.

**Где подключить.** По каждому месту: показать ошибку в интерфейсе рядом с полем, расчёт не запускать.

- `src/lib/calculator.js`, `calculateB2C` — площадь, комнаты, санузлы;
- `src/lib/calculator.js`, `calculateB2B` — площадь, `bathrooms`, `workplaces`, `meetingRooms`;
- `src/lib/spec-calculator.js`, `SpecCalc.compute` — площадь, санузлы, окна, комнаты;
- `src/lib/office-calculator.js` — площадь и все счётные поля;
- компоненты ввода: `B2CQuizPage.jsx`, `B2CDetailPage.jsx`, `B2BQuizPage.jsx`, `B2BOfficePage.jsx`, `B2BOfficeDetailPage.jsx`.

Расчётные функции при невалидном входе должны возвращать `{ ok: false, error }`, а не бросать исключение и не считать по дефолту.

---

## §2.3 Порядок начисления перепланировки

### Что подтверждено

Находка верна, но механика не та, что описана в отчёте. В `src/lib/spec-calculator.js` уже есть разделение на две сущности:

```js
const REPLAN_SURCHARGE = {
  no:    { fixed: 0,     pct: 0,    perM2: 0,   perRoom: 0 },
  light: { fixed: 80000, pct: 0,    perM2: 0,   perRoom: 0 },
  full:  { fixed: 80000, pct: 0.05, perM2: 500, perRoom: 15000 },
};
```

`approvalCost = fixed + perM2 × площадь + perRoom × (комнат − 1)` это согласование в МЖИ, `worksCost = grandTotal × pct` это работы по перепланировке.

Дефект один: `worksCost` берётся от суммы, в которую уже добавлен премиальный резерв 5%. Прогон на премиуме 60 м², 2 комнаты: двойной счёт составляет **100 119 ₽**. В отчёте Искры указано 118 866 ₽, цифра завышена.

### Решение владельца

Порядок **оставить как есть**. Зафиксировать константой с комментарием, чтобы в будущем никто не «починил» молча:

```js
// Надбавка за перепланировку считается от суммы, включающей премиальный резерв.
// Это осознанное решение владельца от 18.09.2026, а не недосмотр.
// Двойной счёт (5% от 5%) на премиуме 60 м² составляет ~100 000 ₽ и принят.
const RESERVE_BEFORE_REPLAN = true;
```

Синхронизировать описание в `РЕЗЮМЕ-ПРОЕКТА.md`, где порядок описан иначе.

### Свести квиз к модели детальной сметы

Квиз считает перепланировку другим способом: `REPLAN_MOD` множит **весь итог** на 1,03 для лёгкой и 1,16 для полной (якорь `export const REPLAN_MOD` в `src/lib/calculator.js`). На одних и тех же входных капитальный ремонт 60 м² даёт 5 013 104 ₽, и квиз накидывает за полную перепланировку около 800 000 ₽, а детальная смета 375 655 ₽, из которых 125 000 согласование и 250 655 работы. Расхождение вдвое на один и тот же вопрос.

Решение владельца: квиз привести к модели детальной сметы. Множитель `REPLAN_MOD` заменить на ту же надбавку «фиксированная сумма плюс процент», выдаваемую вилкой, а не точной цифрой. Границы вилки сохранить теми же, какими они получаются сейчас из `lowPerM2`/`highPerM2`.

---

## §2.10 Экранирование в печатных отчётах

### Что проверено и что оказалось неверным

Функция `esc()` существует в обоих файлах и применяется к большинству подстановок, включая заголовок страницы: `<title>${esc(title)} — РПКМ</title>`, причём у всех четырёх генераторов заголовок имеет запасное значение. **Утверждение отчёта про `<title>Смета — undefined</title>` не подтвердилось.** Функций `buildSpecHTML`, `buildEstimateHTML`, `buildChecklistHTML`, на которые ссылается отчёт, в коде нет вовсе: экспортируются `generateB2CSummaryReportHTML`, `generateB2CDetailReportHTML`, `generateB2BReportHTML`, `generateB2BOfficeReportHTML` и `generateReportHTML`.

Реально не экранированы два места.

**Фотография в акте приёмки.** `src/lib/checklistReport.js`, якорь `<img src="${p.photo}"`. Значение приходит из `localStorage` без проверки. Кавычка в нём разрывает атрибут и позволяет дописать произвольные атрибуты тега.

Про серьёзность честно: наполняет это хранилище сам пользователь на своём же устройстве, поэтому пострадать он может только от себя. Это скорее дефект корректности, чем дыра в безопасности. Чинить всё равно стоит, потому что стоит одну строку, а при будущей синхронизации чек-листов между устройствами риск станет настоящим.

**Иконки групп.** `src/lib/estimateReport.js`, четыре места, якоря `${g.icon || ''}` и `${sec.icon || ''}`. Сейчас там захардкоженные эмодзи из файлов данных, риска нет. Экранировать на будущее.

### Что сделать

Для фото:

```js
const safePhoto = typeof p.photo === 'string' && p.photo.startsWith('data:image/') ? p.photo : '';
```
и подставлять `safePhoto`, пустое значение означает пропуск тега `<img>` целиком.

Для иконок: обернуть в существующую `esc()`.

---

## Не трогать

- Сами расценки и составы работ в `src/data/` и `src/lib/spec-data.js`. Это ТЗ про арифметику и валидацию, не про цены.
- Премиальный тир и состав обстановки: мебель, техника, декор. Для них отдельное ТЗ с галочкой и привязкой к площади.
- Серверную часть. Расчёт живёт на клиенте, эндпоинт `/api/calc` пока не существует.
- Вёрстку страниц результата вне мест, куда добавляются сообщения об ошибках ввода.

---

## Критерии приёмки

1. Скачок на границе эталона исчез. Прогон по `S1` даёт 19 225 → 32 196, 19 225,85 → 32 195, 19 226 → 32 194, 19 227 → 32 193 ₽/м², без ступеньки. Тот же прогон для «Бизнеса» вокруг 43 671,88 м² тоже идёт без разрыва.
1a. `grep -n "Math.ceil" src/pages/B2BOfficeDetailPage.jsx` не находит ни одной строки в блоке масштабирования объёмов.
1b. При площади меньше 10 000 м² над таблицей появляется заметный блок с предупреждением и процентом завышения; при площади меньше 3 000 м² текст блока жёстче. Оба проверяются глазами в браузере, скриншоты в отчёт.
2. `formatRub(undefined)`, `formatRub(null)`, `formatRub(NaN)`, `formatRub(Infinity)` возвращают `'—'` и не бросают исключений.
3. Ни одна из расчётных функций при площади `0`, `-50`, `'abc'`, `undefined`, `NaN` не возвращает денежную сумму. Прогон повторяет таблицы из §2.2, во всех строках ожидается отказ, а не число.
4. `calculateB2B` с `bathrooms: 'abc'` не возвращает `null` в денежных полях, и страница результата не падает.
5. Отрицательная площадь не даёт отрицательных сумм ни в одном калькуляторе.
6. Константа `RESERVE_BEFORE_REPLAN` присутствует с комментарием, описание в `РЕЗЮМЕ-ПРОЕКТА.md` ей соответствует.
7. Квиз и детальная смета на одинаковых входных данных дают согласованную надбавку за перепланировку: вилка квиза накрывает цифру детальной сметы.
8. `grep -n 'src="\${p.photo}"' src/lib/checklistReport.js` даёт пусто.
9. `npx vite build` проходит.

---

## Поправки к отчёту Искры, которые надо занести в документы

По ходу проверки нашлись расхождения. Их стоит отметить в закрывающем отчёте, чтобы следующая итерация не опиралась на неверное.

1. §2.3: двойной счёт составляет 100 119 ₽, а не 118 866 ₽.
2. §2.2: `calculateB2B` на мусорном `bathrooms` возвращает `null`, а не `NaN`. Отсюда падение `formatRub`, которого отчёт не заметил.
3. §2.10: `<title>` экранирован и имеет запасные значения, утверждение про `undefined` в заголовке неверно. Имена функций в отчёте не соответствуют коду.
3a. §2.4: дефект реален, но причина названа неверно. Округление площадных объёмов даёт 0,2% скачка, остальные 99,8% приходятся на дискретные позиции, из них 70% на те, что существуют в единственном экземпляре. Предложенный аудитом фикс («ceil только для штук и комплектов») не сработал бы: проблемные позиции и есть штуки и комплекты.
4. §3.3: `subscriptions.expires_at` объявлен как `TIMESTAMPTZ`, а не TEXT. Закрыто 18.09.2026.
5. §3.2: константы `B2B_DEMO_AVAILABLE` в коде нет. Закрыто 22.09.2026, см. `docs/REPORT_PRO_gate.md`.
