# ТЗ: уровни детальной сметы — состав вместо коэффициентов

## Проблема

Детальная смета (`SpecCalc.compute`, `src/lib/spec-calculator.js`) даёт для квартиры
60 м², новостройка-монолит, без отделки, полный цикл:

> **Поправка от 23.09.2026, важнее всего остального в этом документе.**
> Проценты ниже по тексту считались от вилки квиза, полученной на неизвестном
> наборе ответов, и оказались завышены примерно на десять пунктов. Вилка квиза
> не константа: `TIERS` для капитального это 35 000 — 55 000 ₽/м², а дальше на
> них накручиваются модификаторы (тип дома, коммуникации, дизайн-проект, сроки,
> отделка от застройщика), плюс стоимость окон добавляется отдельной суммой.
> Разные правдоподобные наборы ответов дают верх вилки от 59 до 70 тысяч ₽/м².
>
> **Эталонный случай, утверждён Дмитрием 23.09.2026.** Все сравнения вилки и
> детальной сметы — только на нём, и в отчётах он выписывается явно:
> новостройка-монолит (`house_type: 'nov_monolith'`), без отделки от застройщика
> (`finish_type: 'no_finish'`), 60 м², 1 санузел, 3 окна, 2 комнаты, коммуникации
> полные (`comms: 'full'`), дизайн-проекта нет (`design: 'no'`), сроки не срочные
> (`timing: 'flexible'`), перепланировки нет, режим `full`.

| | ₽/м² |
|---|---|
| Квиз `capital` на эталонном случае | 41 638 — 65 431 |
| Детальная `capital` до переделки | 82 550 |

Превышение верха вилки — **+26 %**. Цель была «10–15 % сверху», но на верной
основе она потеряла смысл: смета, лежащая внутри рыночной вилки, лучше, чем
смета выше неё. Фактическая цель — привести капитальный в вилку, не занижая
расценки.

Причина не в ценах позиций: расценки взяты из реальных тендерных смет и верны.
Причина в **составе**. `SPEC_DATA.full` — это один конкретный объект (евроремонт),
где все 180 м² стен обшиты ГКЛ по каркасу в два слоя и дополнительно утеплены,
а потолок — подвесной ГКЛ. `TIER_MULTIPLIERS` при этом трогает только `finish` и
`sanitary`, а 41 % денег лежит в `rough`:

| класс | ₽ | доля |
|---|---|---|
| rough (конструктив) | 2 034 890 | 41,1 % |
| finish | 998 807 | 20,2 % |
| prep (подготовка) | 693 183 | 14,0 % |
| doors | 456 527 | 9,2 % |
| terminal (оконечка) | 315 198 | 6,4 % |
| windows | 180 336 | 3,6 % |
| engineering (магистрали) | 174 049 | 3,5 % |
| sanitary (магистрали) | 100 000 | 2,0 % |

**Доказательство, что одними коэффициентами задача не решается.** Понижающие
на `prep`, материалы `finish` и материалы `terminal` — даже жёсткие — дают:

| коэффициенты | результат | к верху вилки |
|---|---|---|
| prep 0,85 / finish mp 0,80 / term mp 0,70 | 79 387 ₽/м² | +33 % |
| prep 0,75 / finish mp 0,65 / term mp 0,55 | 77 322 ₽/м² | +30 % |
| prep 0,65 / finish mp 0,50 / term mp 0,45 | 75 365 ₽/м² | +27 % |

Ниже +27 % коэффициенты не опускают, потому что деньги не там. А чтобы
опустить сильнее, пришлось бы занижать сами расценки — то есть врать в цифре,
которую пользователь потом понесёт подрядчику.

**Что решает.** Смена состава `rough` для уровня «Капитальный»:

| вариант | результат | к верху вилки |
|---|---|---|
| только убрать утепление стен | 76 246 ₽/м² | +28 % |
| ГКЛ стен → штукатурка, утепление оставить | 73 822 ₽/м² | +24 % |
| **ГКЛ стен → штукатурка + убрать утепление** | **67 518 ₽/м²** | **+3,2 %** |
| то же + потолок без ГКЛ | 62 797 ₽/м² | +6 % |

Третий вариант попадает в целевой коридор при коэффициентах = 1. То есть
коэффициенты из этого ТЗ становятся точной подстройкой, а не основным рычагом.

---

## Решение по составу (подтверждено Дмитрием 23.09.2026)

> Для уровня «Капитальный» в жилых помещениях стены **штукатурятся по маякам**
> (расценка берётся из существующей позиции «Штукатурка стен толщиной до 20 мм
> по сетке», 2 486 ₽/м² работа+материал), а не обшиваются ГКЛ по каркасу;
> **утепление стен не выполняется**; потолок остаётся подвесным ГКЛ.

Подтверждено. Расценку штукатурки брать из существующей позиции, новых цифр
не придумывать.

---

## Часть 1. Два новых класса работ

Сейчас в позициях `SPEC_DATA.full` шесть классов (`cls`): `rough`, `finish`,
`sanitary`, `engineering`, `doors`, `windows`. Нужно восемь.

### 1.1. `prep` — подготовка поверхностей под финишный материал

Выделяется из `rough`. Переклассифицировать в `src/lib/spec-data.js`
(`SPEC_ITEMS_FULL`, при наличии тех же позиций — и в `SPEC_ITEMS_WHITEBOX`):

| поз. | name | mat / примечание |
|---|---|---|
| 20 | Грунтовка потолка | жилые, 52,5 м² |
| 21 | Шпатлевка потолка в 2 слоя | жилые |
| 24 | Грунтовка стен | жилые, 180 м² |
| 25 | Шпатлевка стен за 2 раза | жилые |
| 26 | Оклейка стен стеклохолстом | жилые |
| 38 | Грунтовка потолка | санузел, 7,5 м² |
| 39 | Шпатлевка потолка в 2 слоя | санузел |
| 42 | Грунтовка стен | санузел, 36 м² |
| 43 | Штукатурка стен толщиной до 20 мм по сетке | санузел |

Номера позиций — по порядку следования в массиве `SPEC_ITEMS_FULL` (1-based).
Сверяться по `name` + `unit` + `vol`, не по индексу.

В `rough` остаётся конструктив: перегородки, потолки из листовых материалов,
облицовка стен ГКЛ, утепление, стяжки, гидроизоляция, стеклянные перегородки.

### 1.2. `terminal` — оконечка

Выделяется из `engineering` и `sanitary`. Логика: магистрали (трубы, кабель,
щит, распаечные коробки) — это инженерия, она почти не зависит от уровня
отделки; оконечка — это приборы и фурнитура, её стоимость целиком определяется
выбором заказчика.

| поз. | name | откуда |
|---|---|---|
| 1 | Монтаж умывальника, раковины с сифоном | sanitary |
| 2 | Монтаж смесителя | sanitary |
| 3 | Монтаж смесителя (гигиенический душ) | sanitary |
| 4 | Монтаж смесителя (душевой комплект) | sanitary |
| 6 | Монтаж полотенцесушителя электрического | sanitary |
| 7 | Монтаж унитаза | sanitary |
| 8 | Монтаж инсталяции | sanitary |
| 9 | Монтаж ванны | sanitary |
| 11 | Монтаж светильника светодиодного | engineering |
| 12 | Монтаж выключателя или переключателя | engineering |
| 13 | Монтаж розетки для скрытой проводки | engineering |
| 15 | Монтаж рамки | engineering |

В `sanitary` остаются: прокладка труб ХВС/ГВС, прокладка труб канализации.
В `engineering` остаются: прокладка кабельных линий в гофре, монтаж коробки
электротехнической, монтаж вводного щита.

> **Поз. 8 «Монтаж инсталяции»** — решение Дмитрия 23.09.2026: при капитальном
> ремонте это оконечка, класс `terminal`. Вопрос закрыт.

### 1.3. Код

`src/lib/spec-calculator.js`: в `TIER_MULTIPLIERS` каждый уровень должен
перечислять все восемь классов. Фолбэк `tierMult[cls] || tierMult.rough`
оставить — он страхует от позиции с неизвестным `cls`, но после переклассификации
срабатывать не должен.

---

## Часть 2. Переименование уровней

Текущий набор `SPEC_DATA.full` — это **евроремонт**, а не капитальный. Сейчас он
подписан как `capital` с множителями 1,0, а `euro` получает надбавку сверху.
Это перевёрнуто.

Целевая модель:

| tier | набор позиций | множители |
|---|---|---|
| `cosmetic` | **новый набор** (часть 4) | все 1,0 |
| `capital` | `full` со сменой состава (часть 3) + понижающие | часть 3 |
| `euro` | `full` как есть | все 1,0 |
| `premium` | `SPEC_DATA.premium` как есть | без изменений |

`TIER_LABELS` дополнить: `cosmetic: 'Косметический'`.

**Обратная совместимость.** `tierKey` в `SpecCalc.compute` сейчас падает на
`'capital'` при неизвестном уровне — оставить. Квиз (`B2CQuizPage.jsx`) мапит
`repair_type` через `tierMap`, где `cosmetic → capital`; после этого ТЗ маппинг
должен стать прямым: `cosmetic → cosmetic`.

---

## Часть 3. Уровень «Капитальный»

Строится из того же `SPEC_DATA.full` двумя механизмами.

### 3.1. Смена состава

Нужен механизм подмены позиций по уровню. Предлагаемая форма — отдельный
экспорт в `src/lib/spec-data.js`, чтобы не дублировать массив позиций:

```js
export const TIER_COMPOSITION = {
  capital: {
    // позиции, исключаемые из набора (сверка по name + unit)
    exclude: [
      { name: 'Облицовка стен из гипсокартона/фанеры в два слоя по металлическому каркасу', unit: 'м2' },
      { name: 'Устройство утепления стен', unit: 'м2' },
    ],
    // позиции, добавляемые взамен
    add: [
      { g:'05.05.02.06', sub:'05.05.02.06.02', name:'Штукатурка стен толщиной до 20 мм по сетке',
        mat:'Штукатурная смесь, сетка, маяки', unit:'м2', vol:'area*3', wp:<из поз.43>, mp:<из поз.43>, cls:'rough' },
    ],
  },
};
```

`wp`/`mp` для добавляемой позиции **взять ровно из существующей поз. 43**
(штукатурка в санузле) — не придумывать новые цифры. `vol` — та же формула,
что у исключаемой облицовки ГКЛ (сверить по факту в данных, ожидается `area*3`).

В `SpecCalc.compute` применять `TIER_COMPOSITION[tierKey]` к `items` до цикла
расчёта. Для уровней без записи в `TIER_COMPOSITION` набор не меняется.

### 3.2. Понижающие коэффициенты

**Решение Дмитрия от 23.09.2026, принято по прогону вариантов — мягкий вариант:**

```js
capital: {
  rough:       { wp: 1.00, mp: 1.00 },
  prep:        { wp: 0.90, mp: 0.90 },   // 1 слой шпатлёвки вместо 2, без стеклохолста
  finish:      { wp: 1.00, mp: 0.85 },   // финишный материал дешевле
  terminal:    { wp: 1.00, mp: 0.80 },   // оконечка эконом-сегмента
  engineering: { wp: 1.00, mp: 1.00 },
  sanitary:    { wp: 1.00, mp: 1.00 },
  doors:       { wp: 1.00, mp: 1.00 },
  windows:     { wp: 1.00, mp: 1.00 },
}
```

Результат: **65 344 ₽/м²**, то есть **−0,1 % к верху вилки квиза** на эталонном
случае (верх вилки 65 431 ₽/м²). Капитальный оказывается внутри рыночной вилки,
у самой её верхней границы.

**Контекст решения, чтобы его потом не переигрывали вслепую.** Смена состава
(часть 3.1) сама по себе дала 67 518 ₽/м², то есть +3,2 % к верху вилки, то есть
почти привела капитальный в вилку без единого коэффициента. Поэтому эти множители не служат
попаданию в цифру: они выражают то, что на капитальном ремонте финишный материал
и оконечка действительно дешевле, чем на евроремонте. Прогонялись варианты
мягкий (65 344 ₽/м²), средний (64 160) и жёсткий (62 976). Выбран мягкий —
решение Дмитрия от 23.09.2026, подтверждено повторно после пересчёта на
эталонном случае: множители обоснованы тем, что финишный материал и оконечка на
капитальном действительно дешевле, чем на евроремонте, и это не зависит от того,
какой знаменатель мы берём для процентов.

Для `euro` все восемь классов = 1,00 (текущие надбавки 1,30/2,00 снимаются —
теперь `full` и есть евроремонт).

---

## Часть 4. Уровень «Косметический» — новый набор позиций

Расценки и состав получены от Дмитрия 23–24.09.2026, все решения ниже — его.

### Что делаем

Косметический ремонт — обновление без вскрытия конструкций: заделка трещин,
шпатлёвка в один слой, покраска, замена финишного покрытия пола и части
оконечки. Двери, окна, плитка на стенах санузла, конструктив и магистрали
не трогаются.

Своего набора позиций у уровня сейчас нет: `tierMap` схлопывает `cosmetic`
в `capital`, и человек, выбравший косметический, получает капитальный.

### 4.1. Новый набор `SPEC_ITEMS_COSMETIC`

В `src/lib/spec-data.js`, экспортируется как `SPEC_DATA.cosmetic`.
Двадцать три позиции. Группы (`g`) берутся существующие из `SPEC_GROUPS`,
новых не заводить.

**Жилые помещения, `g: "05.05.02.06"`**

| name | mat | unit | vol | wp | mp | cls |
|---|---|---|---|---|---|---|
| Заделка трещин | Ремонтный состав, серпянка | пог.м | `(area*3)*0.2` | 1500 | 200 | prep |
| Грунтовка стен | Грунтовка полимерная Knauf Тифенгрунд | м2 | `area*3` | 315 | 13.84 | prep |
| Шпатлевка стен в 1 слой | Шпатлевка гипсовая | м2 | `area*3` | 748.69 | 46.12 | prep |
| Окраска стен за 2 раза | Краска водоэмульсионная Dulux | м2 | `area*3` | 1375.97 | 230.6 | finish |
| Грунтовка потолка | Грунтовка воднодисперсионная Ceresit CT17 | м2 | `area-(sanitary*7.5)` | 315 | 83.01 | prep |
| Шпатлевка потолка в 1 слой | Шпатлевка полимерная | м2 | `area-(sanitary*7.5)` | 768.93 | 46.12 | prep |
| Окраска потолков за 2 раза | Краска водоэмульсионная Dulux | м2 | `area-(sanitary*7.5)` | 1375.97 | 230.6 | finish |
| Демонтаж существующего покрытия пола | Услуги демонтажа и вывоза | м2 | `area-(sanitary*7.5)` | 350 | 0 | rough |
| Устройство выравнивающей стяжки 10 мм | Ровнитель для пола | м2 | `area` | 900 | 350 | rough |
| Устройство покрытия пола ламинатом | Ламинат 43 класс FineFloor FF-2096 Дуб Каламос | м2 | `area-(sanitary*7.5)` | 2225.84 | 1200 | finish |
| Устройство ПВХ плинтуса | Плинтус ПВХ Dollken НК 100 мм | пог.м | `(area-(sanitary*7.5))*1.5` | 801.3 | 700.09 | finish |

**Санузел, `g: "05.05.02.08"`**

| name | mat | unit | vol | wp | mp | cls |
|---|---|---|---|---|---|---|
| Демонтаж плитки пола | Услуги демонтажа и вывоза | м2 | `sanitary*7.5` | 1000 | 0 | rough |
| Устройство покрытия пола плиткой | Плитка керамогранитная Уральский гранит ID9063E101MR Gerda White 600x600мм | м2 | `sanitary*7.5` | 4856.37 | 990 | finish |
| Грунтовка потолка | Грунтовка воднодисперсионная Ceresit CT17 | м2 | `sanitary*7.5` | 315 | 83.01 | prep |
| Шпатлевка потолка в 1 слой | Шпатлевка полимерная | м2 | `sanitary*7.5` | 768.93 | 46.12 | prep |
| Окраска потолков за 2 раза | Краска водоэмульсионная Dulux | м2 | `sanitary*7.5` | 1375.97 | 230.6 | finish |

**Сантехника, `g: "05.04.02"`** — оконечка берётся **списком, а не классом целиком**.
Решение Дмитрия: при косметическом меняют только унитаз и раковину; ванна,
инсталляция, полотенцесушитель, гигиенический душ и душевой комплект остаются,
их замена требует вскрытия и косметикой не является.

| name | mat | unit | vol | wp | mp | cls |
|---|---|---|---|---|---|---|
| Монтаж умывальника, раковины с сифоном | Раковина | шт | `sanitary` | 4451.67 | 4000 | terminal |
| Монтаж смесителя | Смеситель | шт | `sanitary` | 6070.46 | 3000 | terminal |
| Монтаж унитаза | Унитаз | шт | `sanitary` | 9712.74 | 17000 | terminal |

**Электрика, `g: "05.04.08"`**

| name | mat | unit | vol | wp | mp | cls |
|---|---|---|---|---|---|---|
| Монтаж светильника светодиодного | Светильники светодиодные | шт | `Math.ceil(area/4)` | 3237.58 | 2500 | terminal |
| Монтаж выключателя или переключателя для скрытой проводки | Выключатель для скрытой проводки | шт | `Math.ceil(area/4)` | 720.37 | 415.07 | terminal |
| Монтаж розетки для скрытой проводки | Розетка для скрытой проводки | шт | `Math.ceil(area/4)` | 728.46 | 368.96 | terminal |
| Монтаж рамки | Рамки для розеток и выключателей | шт | `(Math.ceil(area/4))*2` | 283.29 | 138.36 | terminal |

Поля `sub` заполнить по образцу соседних позиций той же группы в
`SPEC_ITEMS_FULL`.

### 4.2. Откуда взялись новые расценки

В комментарии над массивом зафиксировать происхождение, иначе через месяц
никто не восстановит:

- Заделка трещин 1500 + 200 ₽/пог.м, объём 0,2 пог.м на м² стен под покраску.
- Демонтаж покрытия пола 350 ₽/м² (ламинат) — только работа. Паркет стоил бы
  800, но для косметики принят ламинат как типовое покрытие вторички.
- Демонтаж плитки 1000 ₽/м² — только работа, только пол санузла.
- Выравнивающая стяжка 10 мм: 900 работа + 350 материал.
- Шпатлёвка в один слой — **ровно половина двухслойной расценки той же
  поверхности**: стены 1497,38 / 92,24 → 748,69 / 46,12; потолки
  1537,86 / 92,24 → 768,93 / 46,12. Если двухслойные расценки когда-нибудь
  пересматриваются, однослойные пересчитываются от них, а не правятся отдельно.

### 4.3. Движок и интерфейс

- `SpecCalc.compute`: при `tier === 'cosmetic'` набор — `SPEC_DATA.cosmetic`,
  независимо от `mode`. WhiteBox для косметики смысла не имеет: застройщик
  сделал стяжку и штукатурку, а мы обновляем старую квартиру.
- `TIER_MULTIPLIERS.cosmetic` — все восемь классов по 1,0. Набор уже собран
  под уровень, множители ему не нужны.
- `TIER_LABELS.cosmetic` — уже есть, не трогать.
- `src/data/specTier.js`: `tierMap.cosmetic` меняется с `'capital'` на
  `'cosmetic'`. Это тот самый отложенный пункт, теперь ему есть куда вести.
- `src/pages/B2CDetailPage.jsx`, массив карточек выбора уровня (сейчас три
  карточки, якорь — строка с `key: 'capital'`): добавить **первой** карточку
  косметического:

  ```js
  { key: 'cosmetic', label: 'Косметический', sub: 'Обновление без вскрытия: шпатлёвка в один слой, покраска, замена пола и оконечки. Двери, окна, плитка на стенах и инженерия не трогаются.' },
  ```

### Не трогать

- Наборы `full`, `whitebox`, `premium` и их расценки.
- `TIER_COMPOSITION` — смена состава капитального к косметическому отношения
  не имеет.
- `REPLAN_PARTITION_FACTOR`: в косметическом наборе позиций с перегородками
  нет вовсе, надбавка за перепланировку при этом продолжает работать как у
  остальных уровней.
- Расценки двухслойной шпатлёвки — однослойные выводятся из них, а не заменяют их.

### Критерии приёмки

Эталонный случай для косметического отличается одним ответом — **коммуникации
«не нужна»** вместо «полные». Решение Дмитрия от 24.09.2026: человек, делающий
косметику, коммуникации не меняет, и сравнивать его с вилкой, посчитанной для
полной замены инженерии, некорректно. Остальные уровни сравниваются по-прежнему
с `comms: 'full'`.

1. `SpecCalc.compute` с `tier: 'cosmetic'`, 60 м² / 1 с/у / 3 окна / 2 комнаты,
   `mode: 'full'`, `replan: 'no'` → **1 341 074 ₽, 22 351 ₽/м², 23 позиции**,
   работы 1 036 022 ₽, материалы 305 054 ₽.

   > Цифра исправлена 24.09.2026 с 1 341 076 ₽ на 1 341 074 ₽. Автор ТЗ получил
   > её сложением работ и материалов (1 036 022 + 305 054), а движок считает
   > `grand` иначе: по каждой позиции он округляет сумму работ и материалов, а не
   > складывает два округлённых числа. На двух позициях по 7,5 м² («Грунтовка
   > потолка», «Окраска потолков за 2 раза») это даёт по рублю разницы. Эталон
   > здесь движок; расхождение `grand ≠ works + materials` — отдельный дефект
   > ядра, вынесен в часть 6.
2. Вилка квиза `cosmetic` на эталонном случае с `comms: 'none'` — 21 630 —
   32 445 ₽/м². Смета выше нижней границы на 3,3 %.
3. Остальные уровни не сдвинулись: `capital` 3 758 823 ₽, `euro` 4 791 147 ₽,
   `premium` 40 770 069 ₽ на прежних входных с `replan: 'no'`.
4. `grep -rn "cosmetic" src/data/specTier.js` показывает `cosmetic: 'cosmetic'`.
5. В детальной смете четыре карточки уровня, косметическая первая, выбор
   переключает набор позиций.
6. `npx vite build` проходит без ошибок.
7. Отчёт `docs/REPORT_spec_tiers.md` дополнен разделом по части 4.

---

## Часть 5. Объём перегородок зависит от перепланировки

Решение Дмитрия от 23.09.2026, найдено при боевой проверке.

### Проблема

Позиция «Устройство перегородок из листовых материалов с обшивкой с двух сторон
в два слоя по металлическому каркасу с утеплением» имеет объём `area*0.5` —
половину площади квартиры в квадратных метрах перегородки, независимо от ответа
про перепланировку. На 60 м² это 30 м² и 161 844 ₽, на 150 м² будет 75 м².

Человек, выбравший «перепланировка не требуется», платит за возведение новых
перегородок ровно столько же, сколько выбравший полную перепланировку со сносом
и согласованием в МЖИ. Ответ на вопрос квиза на цифру не влияет.

Та же позиция с той же формулой есть в `SPEC_ITEMS_WHITEBOX`. В премиальном
наборе своя пара: «Монтаж перегородок из ПГП Knauf 100мм» (`area*0.8`) и
«Монтаж перегородок ГКЛ в 2 слоя с623» (`area*0.15`), обе тоже ни от чего
не зависят.

### Техническое ограничение

`evalVolume` в `src/lib/spec-calculator.js` передаёт формулам объёма только
четыре переменные: `area`, `sanitary`, `windows`, `rooms` (якорь — строка
`new Function('area', 'sanitary', 'windows', 'rooms', 'Math', ...)`). Ответа про
перепланировку среди них нет, его нужно добавить. Это правка расчётного ядра,
поэтому часть оформлена отдельно, а не «заодно».

### Что сделать

**5.1. Коэффициент перегородок по уровню перепланировки.**

Добавить в `src/data/replan.js` рядом с `REPLAN_SURCHARGE` — это его естественный
дом, файл уже общий для обоих движков:

```js
// Доля от базового объёма перегородок. При «не требуется» перегородки не
// возводятся вовсе, и позиция выпадает из сметы (SpecCalc пропускает строки
// с нулевым объёмом).
export const REPLAN_PARTITION_FACTOR = {
  no:    0,
  light: 0.5,
  full:  1.0,
};
```

Второго экземпляра этих чисел быть не должно — та же причина, что с
`REPLAN_SURCHARGE`.

**5.2. Переменная в контексте формул.**

`src/lib/spec-calculator.js`: передать в `evalVolume` пятую переменную
`replanFactor` со значением `REPLAN_PARTITION_FACTOR[replanKey]`. `replanKey`
в `SpecCalc.compute` уже вычислен и провалидирован, брать его.

Порядок аргументов в `new Function` и в вызове должен совпадать — это частая
ошибка, проверить отдельно.

**5.3. Формулы объёма.**

| набор | позиция | было | стало |
|---|---|---|---|
| `SPEC_ITEMS_FULL` | Устройство перегородок из листовых материалов… | `area*0.5` | `area*0.5*replanFactor` |
| `SPEC_ITEMS_WHITEBOX` | та же позиция | `area*0.5` | `area*0.5*replanFactor` |
| премиум | Монтаж перегородок из ПГП Knauf 100мм | `area*0.8` | `area*0.8*replanFactor` |
| премиум | Монтаж перегородок ГКЛ в 2 слоя с623 | `area*0.15` | `area*0.15*replanFactor` |

Пропорции между ПГП и ГКЛ в премиуме не менять.

### Не трогать

- «Устройство стеклянных перегородок» (`sanitary*1.95`) и «Монтаж стеклянной
  душевой перегородки премиум» (`sanitary*4.5`) — это душевые ограждения,
  к перепланировке отношения не имеют.
- «Штукатурка стен и перегородок Ротбанд» в премиуме (`area*3.5`) — это отделка
  поверхностей, а не возведение перегородок.
- Надбавку за перепланировку (`REPLAN_SURCHARGE`) и порядок её начисления.
- Расценки `wp`/`mp` всех позиций.

### Критерии приёмки

Эталонный случай, 60 м² / 1 санузел / 3 окна / 2 комнаты, `mode: 'full'`:

1. Капитальный, `replan: 'no'` → **3 758 823 ₽ (62 647 ₽/м²)**, позиции
   с перегородками в смете нет вовсе.
2. Капитальный, `replan: 'light'` → **3 919 745 ₽ (65 329 ₽/м²)**, перегородки
   15 м² на 80 922 ₽.
3. Капитальный, `replan: 'full'` → **4 241 700 ₽ (70 695 ₽/м²)**, перегородки
   30 м² на 161 844 ₽ — то есть не изменился относительно текущего поведения.
4. Евроремонт, `replan: 'no'` → **4 791 147 ₽ (79 852 ₽/м²)**.
5. Премиум, `replan: 'no'` → **40 770 069 ₽ (679 501 ₽/м²)**; обе премиальные
   позиции с перегородками отсутствуют.

   > Цифра исправлена 23.09.2026 с 40 770 068 на 40 770 069. Автор ТЗ считал её
   > ярлыком: сумма двух снимаемых позиций × 1,05, то есть с одним округлением
   > в конце. Движок пересчитывает 5 % резерва от новой суммы после вычета, и
   > округление происходит внутри. Эталон здесь движок.
6. `grep -rn "REPLAN_PARTITION_FACTOR" src/` находит объявление в
   `src/data/replan.js` и использование в `src/lib/spec-calculator.js`, больше нигде.
7. `npx vite build` проходит без ошибок.

---

## Часть 7. Косметический уровень в интерфейсе

Найдено 24.09.2026 при независимой проверке части 4. Часть 4 закрыта по цифрам
(`9559163`, `521945a`), но появление четвёртого уровня обнажило поля, которые
показываются и не работают. Решения Дмитрия от 24.09.2026.

Все правки — в `src/pages/B2CDetailPage.jsx`. Движок не трогаем.

### 7.1. «White Box» на косметическом ничего не меняет, но подсвечивается

**Проблема.** Блок «Тип отделки от застройщика» скрыт только для премиума
(строка 184, `{tier !== 'premium' && (`). Для косметического он показан и
кликается, кнопка получает класс `selected` и обещает «Экономия ~44%», а итог
одинаков: 1 341 074 ₽ и при `full`, и при `whitebox`. Причина в движке и она
правильная: `SpecCalc.compute` для косметического берёт `SPEC_DATA.cosmetic`
раньше, чем смотрит на `mode`, потому что набор собран под уровень. Врёт
интерфейс, а не расчёт.

**Что сделать.** Завести рядом со `effectiveMode` список уровней, для которых
режим задан самим набором позиций, и использовать его в обоих местах.

```js
// Уровни, у которых набор позиций задан жёстко — режим застройщика на них не влияет.
const MODE_LOCKED_TIERS = ['premium', 'cosmetic'];
```

- Строка 59: `const effectiveMode = MODE_LOCKED_TIERS.includes(tier) ? 'full' : mode;`
- Строка 184: условие показа блока меняется на `{!MODE_LOCKED_TIERS.includes(tier) && (`,
  комментарий над ним переписать («скрыт для Премиум и Косметического — у них
  набор позиций не зависит от отделки застройщика»).

### 7.2. Перепланировка на косметическом добавляет 192 054 ₽

**Проблема.** Надбавка за перепланировку считается от суммы и к уровню не
привязана. На эталонном случае косметический даёт 1 341 074 ₽ при `replan: 'no'`,
1 421 074 ₽ при `'light'` и 1 533 128 ₽ при `'full'`. В наборе косметического нет
ни перегородок, ни демонтажа стен, ни магистралей. Человек, который ломает стены,
а потом шпатлюет в один слой и кладёт ламинат, физически не существует.

**Решение Дмитрия: прятать выбор**, ровно так же, как он уже спрятан для White
Box. Вариант «перепланировка поднимает уровень до капитального» отклонён.

**Что сделать.**

```js
// Уровни, на которых перепланировка невозможна по составу работ.
const REPLAN_LOCKED_TIERS = ['cosmetic'];
```

- Строка 60: `const effectiveReplan = (effectiveMode === 'whitebox' || REPLAN_LOCKED_TIERS.includes(tier)) ? 'no' : replan;`
- Строка 239: условие показа блока «Перепланировка» меняется на
  `{effectiveMode !== 'whitebox' && !REPLAN_LOCKED_TIERS.includes(tier) && (`.

Состояние `replan` при этом не сбрасываем: человек может переключиться на
капитальный и вернуть свой выбор. Считается по `effectiveReplan`, он уже везде
подставлен.

### 7.3. «Комнат» и «Замена окон» на косметическом мертвы

**Проблема.** Ни одна из 23 позиций косметического набора не использует `rooms`
или `windows` в формуле объёма (проверено: `windows` и `rooms` в объёмах
`SPEC_ITEMS_COSMETIC` не встречаются). `rooms` влиял только через надбавку за
перепланировку, а её в 7.2 убрали. То есть человек заполняет два поля, которые
не двигают цифру вообще.

**Что сделать.**

- Поле «Комнат» (строки 209–213) и поле «Замена окон» (строки 226–235) скрыть
  для уровней из нового списка:

```js
// Уровни, у которых объёмы не зависят от числа комнат и окон.
const ROOMS_WINDOWS_IRRELEVANT_TIERS = ['cosmetic'];
```

  Поле «Замена окон» уже скрыто при `effectiveMode === 'whitebox'` — добавить
  второе условие тем же способом, что в 7.2.
- Строка 61, `paramCount`: сейчас `effectiveMode === 'whitebox' ? 3 : 4`. Для
  косметического остаётся два поля (площадь и санузлы), значит выражение должно
  учитывать и уровень. Вынести в `useMemo` или в чистую функцию рядом — считать
  как число реально показанных числовых полей, а не хардкодом.
- Значения `rooms`/`windows` в состоянии не сбрасывать: они нужны при
  переключении на другой уровень.

### 7.4. Заголовок обещает 50 позиций на всех уровнях

**Проблема.** Строка 167: «Введите {paramCount} параметра — получите смету по
50 позициям». Фактически на эталонном случае: косметический 23 строки,
капитальный 48, евроремонт 49, премиум 118. Пятьдесят не совпадает ни с одним.

**Что сделать.** Убрать число из заголовка либо брать его из `preview.lines.length`,
когда расчёт уже посчитан. Второе честнее и почти бесплатно: `preview` считается
на строке 63 и на момент отрисовки заголовка уже есть. Формулировка на выбор
исполнителя, смысл: «получите смету по {N} позициям», при отсутствии `preview` —
без числа.

### 7.5. Детальная смета всегда открывается на «Капитальном»

**Проблема.** Строка 20: `useState('capital')`. Маршрут `/b2c-detail`
самостоятельный, состояние в него не передаётся: на него приходят из клуба
(`ClubPage.jsx` строки 181 и 289), со страницы быстрой вилки
(`B2CResultPage.jsx` строка 114), со страницы детального результата
(`B2CResultDetailPage.jsx` строка 41) и из середины квиза при ответе
«есть дизайн-проект» (`B2CQuizPage.jsx` строка 131). До части 4 это не било:
`cosmetic` схлопывался в `capital`, и дефолт случайно совпадал с ответом. Теперь
человек, ответивший в квизе «косметический», по умолчанию видит смету в 2,8 раза
дороже своего ответа.

**Что сделать.** Засеять форму из последнего расчёта, если он есть в
`sessionStorage`. Единственный ключ — `rpkm-last-b2c-detail`; его пишут и квиз
(`B2CQuizPage.jsx` строка 122), и сама детальная страница (строка 87), и в обоих
случаях внутри лежит `result` от `SpecCalc.compute`, у которого есть
`result.mode` и `result.inputs` с полями `area`, `sanitary`, `windows`, `rooms`,
`replan`, `tier`. Быструю вилку (`rpkm-last-b2c`) как источник не использовать:
`calculateB2C` возвращает только `tier` и `area`, остальное пришлось бы
домысливать.

Требования к сиду:

1. Один раз при монтировании, до первого взаимодействия. Дальше пользователь
   хозяин формы — повторно не перезаписывать.
2. Каждое значение прогнать через тот же валидатор с теми же границами, что
   стоят в `commitField` для этого поля (площадь `validateNumber` min 20 /
   max 500, санузлы `validateInteger` 1–6, окна 0–20, комнаты 1–10). Значение, не
   прошедшее проверку, игнорируется, поле остаётся на дефолте. Причина: в
   `sessionStorage` может лежать расчёт, сделанный до ужесточения границ в
   `calc_input_hardening`, либо руками подправленный.
3. `tier` принимать только из `['cosmetic','capital','euro','premium']`,
   `mode` — из `['full','whitebox']`, `replan` — из `['no','light','full']`.
   Всё прочее игнорировать.
4. Вместе с закоммиченным значением обновлять и сырой текст поля
   (`areaRaw`, `sanitaryRaw`, `windowsRaw`, `roomsRaw`), иначе в input останется
   дефолтная строка.
5. Весь разбор в `try/catch` — битый JSON в `sessionStorage` не должен ронять
   страницу.

### Критерии приёмки части 7

1. Косметический: блока «Тип отделки от застройщика» на странице нет, блока
   «Перепланировка» нет, полей «Комнат» и «Замена окон» нет. Заголовок называет
   число позиций, совпадающее с числом строк в смете.
2. Капитальный, евроремонт: все четыре блока на месте, поведение прежнее.
   Премиум: как было, режим скрыт.
3. Контрольные цифры не сдвинулись: `cosmetic` 1 341 074 ₽, `capital` 3 758 823 ₽,
   `euro` 4 791 147 ₽, `premium` 40 770 069 ₽ на эталонном случае
   (60 м² / 1 с/у / 3 окна / 2 комнаты, `replan: 'no'`).
4. Переключение косметический → капитальный → косметический не теряет введённые
   площадь, санузлы, комнаты и окна.
5. Пройти квиз с ответом «косметический», нажать «Открыть детальную смету» —
   форма открывается на уровне «Косметический» с площадью из квиза.
6. То же с «премиум» — форма открывается на «Премиум».
7. Зайти на `/b2c-detail` в чистой сессии (`sessionStorage` пуст) — форма
   открывается на прежних дефолтах, ошибок в консоли нет.
8. Положить в `sessionStorage` под ключом `rpkm-last-b2c-detail` строку `{`
   (битый JSON) и открыть страницу — страница работает, форма на дефолтах.
9. `npx vite build` проходит без ошибок.
10. Отчёт `docs/REPORT_spec_tiers.md` дополнен разделом по части 7.

---

## Часть 8. Экран результата говорит не то, что посчитано

Найдено при боевой проверке частей 4 и 7, 24.09.2026. Три пункта, все одного
сорта с частью 7: расчёт верный, а страница показывает не его. Правки в
`src/pages/B2CResultDetailPage.jsx`, `src/lib/estimateReport.js`,
`src/pages/B2BResultPage.jsx`, `src/pages/B2BOfficeResultPage.jsx`,
`src/pages/B2BOfficeDetailPage.jsx` и один новый файл. Движок не трогаем.

### 8.1. Уровень в шапке не назван вообще

**Проблема.** Строка 51 строит подпись только из режима:

```js
const modeLabel = r.mode === 'whitebox' ? 'WhiteBox' : 'Полная отделка';
```

и строка 60 печатает `Детальная смета · {modeLabel}`. Человек получает смету на
1 341 074 ₽ и не видит, на какой ремонт она посчитана. Найдено Дмитрием при
боевой проверке.

При этом в печатной смете уровень есть в четырёх местах (`estimateReport.js`,
строки 106, 239, 271, 337: «Категория: Косметический»). Печатная версия честнее
экранной.

**Решение Дмитрия от 24.09.2026: уровень показываем всегда, режим — только там,
где человек его выбирал.** На косметическом и премиуме режим задан принудительно
(часть 7, `MODE_LOCKED_TIERS`), и писать там «Полная отделка» значит повторять на
следующем экране ту же ложь, которую часть 7 убрала из формы.

**Что сделать.** `B2CResultDetailPage.jsx`, строки 51 и 60:

```js
// Режим показываем только на уровнях, где он влияет на состав и человек его
// выбирал. На косметическом и премиуме он задан жёстко — см. MODE_LOCKED_TIERS
// в B2CDetailPage.jsx, часть 7.
const MODE_SHOWN_TIERS = ['capital', 'euro'];
const modeLabel = r.mode === 'whitebox' ? 'White Box' : 'Полная отделка';
const heading = MODE_SHOWN_TIERS.includes(r.tier)
  ? `Детальная смета · ${r.tierLabel} · ${modeLabel}`
  : `Детальная смета · ${r.tierLabel}`;
```

`r.tier` и `r.tierLabel` уже лежат в результате `SpecCalc.compute` (строка 184
`spec-calculator.js`), добавлять в движок ничего не нужно.

Заодно исправить написание: сейчас в коде `WhiteBox` без пробела, а на карточке
выбора в форме `White Box`. Привести к варианту с пробелом.

**Список уровней дублируется между двумя файлами.** Это сознательно: выносить его
в общий модуль сейчас — лишняя связность ради двух строк. Но оба места помечены
комментарием со ссылкой друг на друга, чтобы правка одного не забыла второй.

### 8.2. Строка параметров показывает «Комнат» и «Окон» на косметической смете

**Проблема.** Строки 83–86 печатают все четыре параметра всегда:

```jsx
<div><div className="meta-item-label">Комнат</div>...{inp.rooms}...</div>
<div><div className="meta-item-label">Окон</div>...{inp.windows}...</div>
```

Часть 7 убрала эти поля из формы, потому что ни одна из 23 позиций
косметического набора не использует `rooms` или `windows` в формуле объёма. На
следующем экране они возвращаются и выглядят как параметры, по которым считали.

**Что сделать.** Скрыть те же два параметра на тех же уровнях, на которых они
скрыты в форме:

```js
// Те же уровни, что ROOMS_WINDOWS_IRRELEVANT_TIERS в B2CDetailPage.jsx (часть 7).
const ROOMS_WINDOWS_IRRELEVANT_TIERS = ['cosmetic'];
const showRoomsWindows = !ROOMS_WINDOWS_IRRELEVANT_TIERS.includes(r.tier);
```

Сетка `result-meta` уже стоит на `repeat(auto-fit, minmax(120px, 1fr))`, то есть
сама перестроится с четырёх колонок на две. Проверить визуально.

**В печатной смете то же самое, проверено 24.09.2026.** `estimateReport.js`,
строки 178 и 180, печатают «Комнат» и «Окон» безусловно. Скрывать их по тому же
правилу и в печатной версии, иначе PDF будет противоречить экрану.

### 8.3. «23 позиций» вместо «23 позиции»

**Проблема.** Вычисленное число склеено с жёстко зашитым словом в семи местах.
Для 48 и 118 верно, для 23 нет. Раздел сметы из трёх строк уже сейчас показывает
«3 позиций».

| файл | строка | что стоит |
|---|---|---|
| `src/pages/B2CResultDetailPage.jsx` | 63 | `{r.lines.length} позиций` |
| `src/pages/B2CResultDetailPage.jsx` | 108 | `{g.lines.length} позиций` |
| `src/lib/estimateReport.js` | 170 | `${r.lines.length} позиций` |
| `src/lib/estimateReport.js` | 239 | `${specResult.lines.length} позиций` |
| `src/pages/B2BResultPage.jsx` | 157 | `{specResult.lines.length} позиций` |
| `src/pages/B2BOfficeResultPage.jsx` | 154 | `{sec.lines.length} позиций` |
| `src/pages/B2BOfficeDetailPage.jsx` | 521 | сумма по группам + `позиций` |

**Что сделать.** Завести `src/lib/plural.js` — хелпера склонения в проекте нет
(проверено грепом):

```js
// Русское склонение существительного при числительном.
// forms: [1 позиция, 2 позиции, 5 позиций]
export function plural(n, forms) {
  const a = Math.abs(n) % 100;
  const b = a % 10;
  if (a > 10 && a < 20) return forms[2];
  if (b > 1 && b < 5) return forms[1];
  if (b === 1) return forms[0];
  return forms[2];
}

export const positions = n => `${n} ${plural(n, ['позиция', 'позиции', 'позиций'])}`;
```

Заменить все семь мест на `positions(n)`. Контрольные значения: 1 позиция,
3 позиции, 5 позиций, 11 позиций, 21 позиция, 23 позиции, 48 позиций,
111 позиций, 118 позиций.

**Не трогать** `src/components/ProPaywall.jsx:34` — там `{positions}+ позиций`,
и «50+ позиций» верно при любом числе, потому что «+» читается как «с лишним».

### Не трогать

- Движок расчёта. Часть 8 целиком про отображение.
- Заголовок формы в `B2CDetailPage.jsx` («получите смету по N позициям») —
  там дательный падеж, «по 23 позициям» верно при любом числе.
- Тексты, где число не вычисляется, а написано руками: `HomePage.jsx`
  («~50 позиций в смете», «5000+ позиций в базе»), `ClubPage.jsx`
  («50+ позиций»). Это маркетинговые оценки, а не результат расчёта.
- `TIER_LABELS` и состав наборов.

### Критерии приёмки

1. Косметическая смета: шапка «Детальная смета · Косметический», без режима.
   В строке параметров две ячейки, «Площадь» и «Санузлов».
2. Капитальная смета при полном цикле: «Детальная смета · Капитальный ·
   Полная отделка». При White Box: «… · White Box», с пробелом.
3. Премиум: «Детальная смета · Премиум», без режима.
4. Число позиций склоняется верно на всех четырёх уровнях: 23 позиции,
   48 позиций, 49 позиций, 118 позиций. В разделах сметы тоже: раздел из трёх
   строк даёт «3 позиции», из одной — «1 позиция».
5. Печатная смета не потеряла «Категория» ни на одном уровне и склоняет число
   позиций так же, как экран.
6. Контрольные цифры не сдвинулись: `cosmetic` 1 341 074 ₽, `capital`
   3 758 823 ₽, `euro` 4 791 147 ₽, `premium` 40 770 069 ₽ на эталонном случае.
7. `grep -rn "позиций" src/` не находит мест, где число вычисляется и склеивается
   со словом напрямую, кроме исключений из раздела «Не трогать».
8. `npx vite build` проходит без ошибок.
9. Отчёт `docs/REPORT_spec_tiers.md` дополнен разделом по части 8.

---

## Часть 9. Уровень из квиза доезжает до детальной формы

Найдено 24.09.2026 при проверке части 8. Часть 7.5 решала не ту задачу, которую
ставила, и это ошибка автора ТЗ, а не исполнителя.

### Проблема

**Мёртвая ветка в квизе.** В `src/pages/B2CQuizPage.jsx` объявлено состояние
`calcMode` со значениями `'quick'` и `'detail'` (строка 52). `setCalcMode` не
вызывается нигде — проверено грепом по всему `src/`. Значит `calcMode` всегда
`'quick'`, и блок строк 106–125 недостижим. В нём считается `SpecCalc.compute`,
пишется ключ `rpkm-last-b2c-detail` (строка 122) и делается переход на
`/b2c-result-detail`.

Карточка «Детальная смета» на экране выбора режима (строка 155) не трогает
`calcMode`, а просто уводит на `/b2c-detail` — до квиза, когда ответов ещё нет.

**Отсюда первое следствие.** Ключ `rpkm-last-b2c-detail` пишет только сама
страница `/b2c-detail` (строка 127). Сид части 7.5 читает единственный источник,
и источник этот — предыдущий ручной расчёт того же человека. Полезно, но это не
то, ради чего часть 7.5 писалась.

**Отсюда второе.** Критерий 5 части 7 («квиз с ответом косметический открывает
форму на косметическом») был закрыт ложно 24.09.2026. Форма открылась на
косметическом потому, что предыдущий ручной расчёт проверяющего был
косметическим, а не потому, что доехал ответ.

**Ошибка в ТЗ части 7.5.** Там написано: «Быструю вилку (`rpkm-last-b2c`) как
источник не использовать: `calculateB2C` возвращает только `tier` и `area`,
остальное пришлось бы домысливать». Рассуждение неверное. `tier` и `area` — это
именно то, что имеет значение; остальные поля не нужно домысливать, они просто
остаются на дефолтах. Единственный работающий источник был отвергнут ради
несуществующего.

**Реальный путь человека**: главная, блок «Категории», карточка уровня
(`HomePage.jsx:208`, переход на `/b2c?tier=<уровень>`) → квиз → `/b2c-result` →
кнопка «Открыть детальную смету» (`B2CResultPage.jsx:114`) → `/b2c-detail`. На
этом пути в `rpkm-last-b2c` лежат уровень и площадь, и сейчас они игнорируются.

**Важное уточнение про уровень.** Квиз про уровень ремонта не спрашивает вовсе:
среди девяти шагов такого нет. `answers.repair_type` приходит только из параметра
`?tier=` в адресе (строки 54–58), который ставит блок «Категории» на главной, а
при заходе на `/b2c` без параметра подставляется `capital`. То есть «ответ квиза
про уровень» — это выбор карточки на главной. Формулировки в критериях ниже
написаны по факту, а не по тому, как это звучало в частях 7 и 8.

### 9.1. Сид читает оба ключа

**Что сделать, файл `src/pages/B2CQuizPage.jsx`.** В лид быстрого расчёта
(строки 98–103) добавить сырые ответы:

```js
const lead = {
  id: 'b2c-' + Date.now(), timestamp: new Date().toISOString(), kind: 'b2c',
  result,
  answers,                       // нужны детальной форме: отделка и перепланировка
  contact: { name: contactData.name, email: contactData.email.trim() },
};
```

Лид уходит только в `sessionStorage`; `sendCalculation` получает отдельный объект
`{ email, name, kind, result }` и ответов не увидит. То есть на сервер и в письмо
ничего нового не уезжает.

**Что сделать, файл `src/pages/B2CDetailPage.jsx`, эффект сида (строки 59–89).**
Читать оба ключа и выбирать свежий по `timestamp`:

- `rpkm-last-b2c-detail` — предыдущий ручной расчёт, даёт все семь полей.
- `rpkm-last-b2c` — быстрая вилка, даёт `tier` и `area` из `result`, а также
  `mode` и `replan` из `answers`.
- Оба лида несут `timestamp` в ISO. Берём тот, чей новее. Если один отсутствует,
  берём второй. Если `timestamp` отсутствует или не парсится, считаем этот лид
  старее.

Из быстрой вилки:

```js
// result.tier уже один из четырёх, но гоняем через toSpecTier: он же покрывает
// euro_top и luxury, если они когда-нибудь станут достижимы через квиз.
tier  = toSpecTier(lead.result.tier);
area  = lead.result.area;
mode  = lead.answers?.finish_type === 'whitebox' ? 'whitebox' : 'full';
replan = lead.answers?.replan;
```

`rooms`, `sanitary` и `windows` из быстрой вилки **не выводить**. Мёртвая ветка
квиза выводила их из площади (`quizRooms`, `quizSanitary`, `quizWindows`,
строки 112–114) — переносить эту эвристику в сид нельзя: это ровно та молчаливая
подстановка чисел, которую убирал `calc_input_hardening`. Человек должен увидеть
дефолт и поправить его, а не получить придуманное число под видом своего.

Валидация, белые списки и `try/catch` — те же, что уже стоят в сиде части 7.5,
без послаблений. `mode` и `replan` из `answers` проверяются по тем же белым
спискам, что и из детального лида.

### 9.2. Мёртвую ветку в квизе удалить

**Что сделать, файл `src/pages/B2CQuizPage.jsx`.**

- Удалить состояние `calcMode`/`setCalcMode` (строка 52).
- Удалить ветку `else` целиком (строки 106–125), оставив тело `if` как
  безусловный код.
- Убрать `calcMode` из массива зависимостей `useCallback` (строка 127).
- Удалить ставшие ненужными импорты `SpecCalc` и `toSpecTier` — после удаления
  ветки они в файле не используются. Проверить грепом, а не на глаз.

**Экран выбора режима не трогаем.** Карточка «Детальная смета», уводящая на
`/b2c-detail`, — живой и правильный путь.

**Зачем удалять, а не оживлять.** Детальная смета закрыта подпиской, а эта ветка
считала бы её прямо в квизе, без единой проверки доступа, и складывала результат
в `sessionStorage`, откуда его рисует `/b2c-result-detail` (у которого своего
гейта нет). Сегодня это безвредно только потому, что ветка недостижима. Такой
код имеет привычку оживать через полгода вместе с дырой.

### Не трогать

- Отсутствие гейта на `/b2c-result-detail` — отдельный долг, записан в роадмап.
  Часть 9 его не закрывает и не усугубляет.
- Эвристики `quizRooms`/`quizSanitary`/`quizWindows` — удаляются вместе с веткой
  и в сид не переносятся.
- Движок расчёта.
- Сид из `rpkm-last-b2c-detail` — он работает и остаётся первым источником, когда
  свежее.

### Критерии приёмки

1. Главная → «Категории» → карточка «Косметический» → пройти квиз → на странице
   результата «Открыть детальную смету» → форма открылась на «Косметическом» с
   площадью из квиза. Проверять в чистой вкладке, иначе сработает предыдущий
   ручной расчёт и критерий снова закроется ложно.
2. То же с карточкой «Премиум» → форма на «Премиум».
3. Квиз с ответом «White Box» и уровнем «Капитальный» → в форме выбран
   «White Box».
4. Квиз с перепланировкой «Полная» и уровнем «Капитальный» → в форме выбрана
   «Полная».
5. Свежесть работает в обе стороны: ручной расчёт на `/b2c-detail`, затем квиз →
   форма берёт данные квиза. Квиз, затем ручной расчёт → форма берёт ручной.
6. Комнаты, санузлы и окна после сида из быстрой вилки остаются 2 / 1 / 3, а не
   выводятся из площади.
7. `grep -n "calcMode\|SpecCalc\|toSpecTier" src/pages/B2CQuizPage.jsx` — пусто.
8. Критерии 7 и 8 части 7 по-прежнему выполняются: чистая сессия даёт дефолты,
   битый JSON под любым из двух ключей не роняет страницу.
9. Контрольные цифры не сдвинулись: `cosmetic` 1 341 074 ₽, `capital`
   3 758 823 ₽, `euro` 4 791 147 ₽, `premium` 40 770 069 ₽ на эталонном случае.
10. `npx vite build` проходит без ошибок.
11. Отчёт `docs/REPORT_spec_tiers.md` дополнен разделом по части 9, и в нём
    отдельно отмечено, что критерий 5 части 7 был закрыт ложно.

---

## Часть 6. `grand` не сходится с суммой колонок

Найдено 24.09.2026 при разборе расхождения в 2 ₽ по косметическому уровню.
**К работе не приступать без отдельной команды Дмитрия** — это правка ядра
расчёта, она затрагивает все четыре уровня и оба контура, и мешать её с частью 4
нельзя.

### Проблема

В `src/lib/spec-calculator.js` по каждой позиции считается три числа:

```js
workCost: Math.round(workCost),
matCost:  Math.round(matCost),
total:    Math.round(workCost + matCost),   // округление суммы, а не суммы округлений
```

`totals.works` и `totals.materials` складывают округлённые `workCost`/`matCost`,
а `totals.grand` складывает `total`. Когда у позиции дробные части работ и
материалов обе около 0,5, округление суммы даёт на рубль меньше, чем сумма
округлений. В итоге напечатанная смета не сходится сама с собой:

| уровень | работы + материалы | grand | разница |
|---|---|---|---|
| Капитальный, `replan: 'no'` | 2 618 945 + 1 139 881 = 3 758 826 ₽ | 3 758 823 ₽ | −3 ₽ |
| Косметический | 1 036 022 + 305 054 = 1 341 076 ₽ | 1 341 074 ₽ | −2 ₽ |

Для сметного инструмента это хуже, чем кажется по величине: пользователь,
проверяющий PDF на калькуляторе, видит, что колонки не бьются с итогом, и
перестаёт доверять всей цифре.

### Что сделать

`src/lib/spec-calculator.js`, блок расчёта позиции (~строки 106–115): сделать
`total` суммой уже округлённых компонентов.

```js
const roundedWork = Math.round(workCost);
const roundedMat  = Math.round(matCost);
...
workCost: roundedWork,
matCost:  roundedMat,
total:    roundedWork + roundedMat,
```

Проверить, не считается ли где-то `total` независимо от `workCost`/`matCost`
(`grep -n "\.total" src/lib/spec-calculator.js src/pages/B2CDetailPage.jsx`), и
одинаково ли ведёт себя `OfficeCalc.compute` — если там та же схема округления,
правку делать симметрично, иначе два движка разъедутся.

### Критерии приёмки

1. Для каждого уровня на эталонном случае `totals.grand === totals.works +
   totals.materials`.
2. Новые контрольные цифры фиксируются в отчёте: они сдвинутся вверх на единицы
   рублей (капитальный 3 758 823 → 3 758 826, косметический 1 341 074 →
   1 341 076). Все критерии предыдущих частей, где стоят старые значения,
   помечаются как пересчитанные — не переписываются молча.
3. `npx vite build` проходит без ошибок.

---

## Не трогать

- Расценки `wp`/`mp` существующих позиций — они из реальных тендерных смет.
  Уровни отличаются составом и множителями, но не подменой самих расценок.
- `SPEC_DATA.premium` и `SPEC_GROUPS_PREMIUM`.
- `src/data/replan.js` и порядок применения резерва/перепланировки
  (`RESERVE_BEFORE_REPLAN`) — сделано в `calc_input_hardening`.
- Квизовый движок `calculateB2C` и вилки `TIERS` в `src/lib/calculator.js` —
  вилка это анализ рынка, она эталон, под неё подгоняется детальная смета,
  а не наоборот.
- `SPEC_DATA.whitebox` — состав меняем только там, где переклассификация `cls`
  (часть 1); уровневой подмены для whitebox не вводим.

## Критерии приёмки

1. В `SPEC_ITEMS_FULL` нет позиций с `cls: 'rough'` из списка 1.1 — у них `cls: 'prep'`;
   нет позиций из списка 1.2 с `cls: 'sanitary'`/`'engineering'` — у них `cls: 'terminal'`.
   Суммарное число позиций не изменилось (50 в `full`).
2. `SpecCalc.compute` с `tier: 'euro'` даёт **тот же итог, что сейчас даёт
   `tier: 'capital'`** — 4 952 991 ₽ (82 550 ₽/м²) для 60 м²/1 с/у/3 окна/2 комнаты,
   полный цикл, без перепланировки. Это контроль, что переименование не сдвинуло цифру.
3. `SpecCalc.compute` с `tier: 'capital'` при множителях 1,0 и включённой сменой
   состава даёт 67 518 ₽/м² ± 100 ₽ на том же наборе входов.
4. `TIER_MULTIPLIERS` для каждого уровня перечисляет все восемь классов явно.
5. `TIER_LABELS.cosmetic === 'Косметический'`; `tierMap` в `B2CQuizPage.jsx`
   больше не схлопывает `cosmetic` в `capital`.
6. `npx vite build` проходит без ошибок.
7. Отчёт `docs/REPORT_spec_tiers.md` с итоговой таблицей ₽/м² по всем уровням
   (`cosmetic`/`capital`/`euro`/`premium`) против вилок квиза для одного и того же
   набора входов.

## Порядок работ

1. Часть 1 (переклассификация) — проверяется критерием 1.

   **Важно, уточнено 23.09.2026 по ходу работы.** Часть 1 не является
   число-нейтральной для уровня `euro`, и это нормально. Восемь из двенадцати
   позиций, уезжающих в `terminal`, раньше лежали в `sanitary`, где у `euro`
   стоит множитель на материалы 2,00; четыре остальные пришли из `engineering`
   с множителем 1,00. Один множитель `terminal` не может сохранить оба
   поведения сразу — по построению. Поэтому после части 1:
   - `capital` обязан остаться **ровно прежним**: 4 952 991 ₽ (82 550 ₽/м²)
     на 60 м² / 1 с/у / 3 окна / 2 комнаты, полный цикл, без перепланировки.
     Его множители и так все единицы, сдвигаться неоткуда. Это и есть контроль
     правильности переклассификации.
   - `euro` сдвинется вниз на 76 451 ₽ (с 5 566 965 до 5 490 514 ₽,
     92 783 → 91 509 ₽/м²) — ровно сумма `mp` восьми перенесённых позиций
     сантехники, потерявших надбавку ×2. Компенсирующий множитель не вводить:
     в части 2 `euro` всё равно приводится к набору с единичными множителями
     и приходит к 4 952 991 ₽. Костыль пришлось бы снимать на следующем шаге.
2. Часть 2 + 3.1 (переименование и смена состава) — проверяется критерием 3.
3. Пауза: я прогоняю варианты коэффициентов, Дмитрий выбирает.
4. Часть 3.2 (коэффициенты) — вписываются выбранные значения.
5. Часть 4 (косметический) — отдельным ТЗ после получения расценок.
6. Часть 5 (перегородки) — сделана 23.09.2026 по ходу боевой проверки.
7. Часть 7 (интерфейс косметического) — сделана 24.09.2026, боевая проверка закрыта.
8. Часть 8 (экран результата) — находки боевой проверки части 7, сделана 24.09.2026.
9. Часть 9 (уровень из квиза) — исправляет ошибку части 7.5.
10. Часть 6 (`grand` vs колонки) — только по отдельной команде и последней:
    она меняет контрольные цифры всех предыдущих частей.
